# Server config
PORT=4000

# Optional: if provided, LLM sentiment analysis is used via Google Gemini.
# If missing, the app falls back to keyword-based sentiment.
Default_Gemini_API_Key=
GEMINI_PROJECT_ID=projects/YOUR_PROJECT_NUMBER
